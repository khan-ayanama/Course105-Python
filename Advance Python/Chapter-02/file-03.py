# Namespace
# Namespace represents memory block where names are mapped to objects.

# Class Namespace: A class maintains it's own namespace known as class namespace. In the class namespae the names aer mapped to class varialbe.

# Instance Namespace: Every instance have it's own namespace known as instance namespace. In the instance namespace, the names are mapped to instance variables.