# Inheritance
# Old class --> Super or Base Class or Parent Class
# Child Class --> Derived Class or Subclass or new class

# All classes in python are build from a single super class caleld 'object' so whenever we create a clasa in python, object will become super class form them internally.
# class Mobile(object): or class Mobile:
# The main advantage of inhertance is code reusability