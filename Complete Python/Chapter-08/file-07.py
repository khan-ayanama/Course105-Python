# LAMBDA FUNCTION

# Lambda functions doesn't have any Name
lambda x:print(x)

# lamba function returns a function
add = lambda x,y:x+y

print(add(2,3))
print(add)

# lambda function can take n number of arguments but only one expression

# In lambda there is no need to write return

# It can only contain expressions and can't include statements in it's body.

# You can use all type of actual arguments